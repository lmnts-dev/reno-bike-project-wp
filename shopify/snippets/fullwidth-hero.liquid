{% comment %}

Fullwidth Hero:
A fullwidth hero component.

@author Peter Laxalt
@since 2/2020

@params
{{ headline }}: Headline
{{ cover }}: Cover image

@todo
Link into Shopify data.

{% endcomment %}

<section class="fullwidth-hero">
  <div class="fullwidth-hero-inner">
    <h1>{{ headline }}</h1>
    
    {% if cover %}
      <img data-src="{{ cover }}" alt="{{ title }}" class="lazy" />
    {% endif %}
  </div>
</section>